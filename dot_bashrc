[ -f ~/.fzf.bash ] && source ~/.fzf.bash

[[ -f ~/.bash-preexec.sh ]] && source ~/.bash-preexec.sh
eval "$(atuin init bash)"

PATH=~/.console-ninja/.bin:$PATH
# Added by LM Studio CLI tool (lms)
export PATH="$PATH:/Users/l1wulf/.cache/lm-studio/bin"
